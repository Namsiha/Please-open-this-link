<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>For Dimpy ❤️</title>

<style>
body {
  margin: 0;
  height: 100vh;
  background: linear-gradient(135deg, #ff4d6d, #ff85a1);
  display: flex;
  justify-content: center;
  align-items: center;
  font-family: Arial, sans-serif;
  overflow: hidden;
}

.card {
  background: white;
  padding: 25px;
  border-radius: 20px;
  text-align: center;
  width: 300px;
  box-shadow: 0 15px 40px rgba(0,0,0,0.25);
}

h2 {
  color: #ff4d6d;
}

#quote {
  font-size: 14px;
  margin: 10px 0;
  color: #555;
}

#tease {
  color: #ff4d6d;
  font-size: 14px;
  margin-top: 10px;
  min-height: 20px;
}

button {
  padding: 10px 22px;
  border: none;
  border-radius: 25px;
  font-size: 16px;
  cursor: pointer;
  margin: 8px;
}

#yes {
  background: #ff4d6d;
  color: white;
  opacity: 0.5;
  cursor: not-allowed;
}

#no {
  background: #ddd;
}

.heart {
  position: fixed;
  bottom: 0;
  font-size: 20px;
  animation: float 5s linear infinite;
}

@keyframes float {
  from { transform: translateY(0); opacity: 1; }
  to { transform: translateY(-100vh); opacity: 0; }
}
</style>
</head>

<body>

<div class="card">
  <h2>Dimpy 💖</h2>
  <p>Will you be my Valentine? 😌💘</p>

  <p id="quote"></p>
  <p id="tease"></p>

  <button id="yes" onclick="yesClicked()">Yes 💕</button>
  <button id="no" onclick="noClicked()">No 🙈</button>
</div>

<script>
let noCount = 0;

const quotes = [
  "You already have my heart, Dimpy ❤️",
  "Every love story is beautiful, but ours is my favourite 😘",
  "I don’t need Valentine’s Day, I just need you 💕",
  "Dimpy + Me = Forever ♾️",
  "My heart smiles when you smile 💖"
];

const teasing = [
  "Awww 😏 don’t be shy",
  "Itna bhaav mat khao Dimpy 🙈",
  "Dil already haan bol chuka hai ❤️",
  "Okay okay… last chance 😌"
];

const quoteEl = document.getElementById("quote");
const teaseEl = document.getElementById("tease");
const yesBtn = document.getElementById("yes");

setInterval(() => {
  quoteEl.innerText = quotes[Math.floor(Math.random() * quotes.length)];
}, 3000);

function noClicked() {
  teaseEl.innerText = teasing[noCount] || teasing[teasing.length - 1];
  noCount++;

  if (noCount >= 2) {
    yesBtn.style.opacity = "1";
    yesBtn.style.cursor = "pointer";
  }
}

function yesClicked() {
  if (noCount < 2) return;

  document.body.innerHTML = `
    <div style="text-align:center;color:white;">
      <h1>YAYYYY 💖🎉</h1>
      <h2>Dimpy, you just made me the happiest 😍💍</h2>
      <p>Forever starts now ♾️</p>
    </div>
  `;
}

function createHeart() {
  const heart = document.createElement("div");
  heart.className = "heart";
  heart.innerText = "💖";
  heart.style.left = Math.random() * 100 + "vw";
  document.body.appendChild(heart);
  setTimeout(() => heart.remove(), 5000);
}

setInterval(createHeart, 300);
</script>

</body>
</html>
